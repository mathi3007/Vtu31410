n=int(input())
students = []
for _ in range(n):
    name, marks = input().split()
    students.append((name, int(marks)))

K = int(input())

students = sorted(students, key = lambda x: (-x[1], x[0]))

top_students = students[:K]

print(" ".join([student[0] for student in top_students]))
